{
  "name": "@lifesystemsufpr/pipeline-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Repo de demonstração do pipeline spec → código → PR → CI da plataforma lifesystemsufpr.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "build": "tsc",
    "pipeline": "tsx pipeline/run-pipeline.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@vitest/coverage-v8": "^4.1.8",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=22"
  }
}
